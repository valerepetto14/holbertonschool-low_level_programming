#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 *   *  main - Entry point
 *     * Return: Always 1 (Success)
 *      **/
int main(void)
{
for (int x = 'a'; x <= 'z'; x++)
    {
	putchar(x);
	putchar('\n');
    }
    return (0);
}
